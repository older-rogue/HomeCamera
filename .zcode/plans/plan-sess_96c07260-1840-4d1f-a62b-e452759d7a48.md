# HomeCamera 代码精简计划（含资源文件清理）

经过对整个代码库的全面分析（3 个并行 Explore 代理 + 关键文件人工复核 + 资源交叉引用验证），以下是完整精简方案。所有改动均经过交叉引用验证，不会破坏现有功能或 21 个单元测试。

## A. 删除 LocalDebug 本地调试功能（约 300 行，跨 5 个文件 + 1 资源）

用户确认完全删除此功能。

1. **`app/src/main/java/com/zx/homecamera/debug/LocalDebugSession.kt`** - 整个文件删除。
2. **`ui/HomeCameraApp.kt`**:
   - 删除 `onLocalDebugViewerSurfaceReady` / `onLocalDebugViewerSurfaceDestroyed` 两个回调参数（94-95 行）
   - 删除 `Screen.LocalDebug` 相关分支：BackHandler 的 137 行、title 的 153 行、showBack 的 156 行、onBackAction 的 160 行、when 渲染的 186-194 行
   - 删除注释掉的 RoleCard 块（266-272 行）
   - 删除 `LocalDebugScreen` composable 整个函数（938-1068 行）
   - 删除未使用的 `import androidx.compose.ui.graphics.Brush`（56 行）
3. **`MainActivity.kt`**:
   - 删除 `PermissionRequestTarget.LocalDebug` 枚举值（221 行），枚举只剩 `Collector`
   - 删除 permissionLauncher 中 LocalDebug 分支（100-101、104-106、114 行）
   - 删除 `HomeCameraAction.EnterLocalDebug` 处理分支（156-159 行）
   - 删除 `onLocalDebugViewerSurfaceReady` / `onLocalDebugViewerSurfaceDestroyed` 传参（180-185 行）
4. **`HomeCameraViewModel.kt`**:
   - 删除 `import LocalDebugSession`（19 行）
   - 删除 `localDebugSession` 字段（51 行）
   - 删除 `BackToRoleSelection` 中的 `localDebugSession?.stop(); localDebugSession = null`（176-177 行）
   - 删除 `EnterLocalDebug` / `ExitLocalDebug` / `LocalDebugStatusChanged` 三个 action 分支（181-193 行）
   - 删除 `startLocalDebugSession` / `stopLocalDebugSession` 两个方法（247-289 行）
   - 删除 `onCleared` 中的 `localDebugSession?.stop(); localDebugSession = null`（499-500 行）
   - 删除未使用方法：`setCollectorPreviewSurface`(235-237)、`clearCollectorPreviewSurface`(239-241)、`setCollectorPreviewSizeListener`(243-245)
5. **`core/app/HomeCameraState.kt`**:
   - 删除 `Screen.LocalDebug` 枚举值（13 行）
   - 删除 `HomeCameraState.localDebug` 字段（88 行）
   - 删除 `EnterLocalDebug` / `ExitLocalDebug` / `LocalDebugStatusChanged` 三个 action（106-108 行）
   - 删除 reducer 中三个对应分支（226-241 行）
   - 删除 `ClientState.errorMessage` 字段（73 行）及 `ScanFailed` reducer 中的写入（194 行）— 此字段从未被任何 UI 或测试读取
6. **`res/drawable/ic_debug.xml`** - 整个文件删除（仅被注释代码引用）。

## B. 删除注释代码块

7. **`ui/theme/Type.kt`** - 删除 18-33 行的注释 Typography 代码块，保留 `bodyLarge`。

## C. 删除未使用的 import

8. **`ui/activity/SplashActivity.kt`** - 删除 17 个未使用 import（RepeatMode/animateFloat/infiniteRepeatable/rememberInfiniteTransition/tween/Image/fillMaxHeight/offset/size/shadow/Brush/painterResource/WindowInsetsCompat/WindowInsetsControllerCompat/R/PrimarySoft 等）。注意：`getValue` 保留（用于 `by current`）。
9. **`ui/activity/IntroActivity.kt`** - 删除 `Arrangement`(8) 和 `Spacer`(12) 两个未使用 import。

## D. 简化死代码分支

10. **`video/H264StreamConfigSelector.kt`** - `fpsFor` 和 `bitrateFor` 的 `else` 分支因 `choose()` 预过滤而不可达，简化为直接返回 `H264StreamConfig.FPS` / `H264StreamConfig.BITRATE`。
11. **`video/Mp4SegmentRecorder.kt`** - 删除未使用的 `recordingRoot` 公开属性（30-31 行）。
12. **`core/protocol/H264UdpPacket.kt`** - 删除未使用的 `MAX_FRAGMENTS_PER_FRAME` 常量（24 行，其他重导出常量被测试使用，保留）。
13. **`network/TcpSubnetScanner.kt`** - 删除未使用的 `import java.net.Socket`（8 行）。

## E. 删除未使用的资源文件（用户强调的重点）

14. **`res/values/colors.xml`** - 删除 6 个模板遗留色：`purple_200`、`purple_500`、`purple_700`、`teal_200`、`teal_700`、`black`（保留 `white`，被 StatusBarUtil 使用）。
15. **`res/xml/backup_rules.xml`** - 整个文件删除（内容仅含注释示例，且 `allowBackup="false"` 使其无效）。
16. **`res/xml/data_extraction_rules.xml`** - 整个文件删除（同上，空规则 + `allowBackup="false"`）。
17. **`AndroidManifest.xml`** - 移除 `android:fullBackupContent="@xml/backup_rules"` 和 `android:dataExtractionRules="@xml/data_extraction_rules"` 两个属性（配合 15、16 删除），保留 `android:allowBackup="false"`。

## F. 清理 git 暂存区残留的垃圾资源（新发现）

18. **`git rm --cached` 两个中文名 PNG**：`手机.png` 和 `监控.png`。
   - 这两个文件是 ic_client.png（2544 字节）和 ic_collector.png（6087 字节）的中文命名副本（字节数完全一致），是之前打算改名但中途放弃的残留。
   - 当前状态：暂存区（staged）里有，工作目录里已删除，从未被提交到 HEAD，也未被任何代码引用。
   - 处理：用 `git rm --cached` 从暂存区移除，使其彻底消失。

## G. 清理构建依赖

19. **`app/build.gradle.kts`** - 删除未使用依赖：
    - `androidTestImplementation(libs.androidx.compose.ui.test.junit4)`（无 Compose UI 测试）
    - `androidTestImplementation(libs.androidx.espresso.core)`（Compose-only 项目不用 Espresso）
    - `implementation(libs.androidx.core)`（已被 core-ktx 覆盖）
    - `debugImplementation(libs.androidx.compose.ui.test.manifest)`（无 Compose 测试需要）
20. **`gradle/libs.versions.toml`** - 同步清理对应的未使用条目（espresso、ui-test-junit4、ui-test-manifest、core 非 ktx）。

## H. 删除 demo 目录

21. 删除 **`demo/index.html`** 及 `demo/` 目录（用户确认删除）。
22. 清理 **`ui/theme/Color.kt`** 中引用 demo 的注释。

## I. 不纳入本次的项

- `需求.txt`、`docs/`、`README.md`、`key.jks`、`app/release/`、`keepRules/rules.keep`、`.gitignore` 的 `/docs/` 条目 - 历史/安全/配置层面，超出"精简代码"范畴。
- `ExampleUnitTest.kt` / `ExampleInstrumentedTest.kt` - 脚手架，能编译通过，保留。
- `手机.png` / `监控.png` 之外的所有 drawable（ic_back/ic_refresh/ic_collector/ic_client/splash_background 各密度/splash_window_background）- 均被代码引用，保留。
- 所有 anim 文件、themes.xml 三个 style、strings.xml - 均被引用，保留。

## 验证步骤

完成所有修改后：
1. 运行 `./gradlew testDebugUnitTest` 确认全部单元测试通过（21 个测试文件均不依赖 LocalDebug，已验证）。
2. 运行 `./gradlew assembleDebug` 确认编译通过。
3. 检查无新增编译警告。

## 影响范围

- 删除代码约 350+ 行（LocalDebug ~300 行 + 其他零散精简）
- 删除 4 个文件（LocalDebugSession.kt、ic_debug.xml、backup_rules.xml、data_extraction_rules.xml）+ demo 目录
- 从暂存区移除 2 个垃圾 PNG（手机.png、监控.png）
- 精简 6 个未使用 import 集合、6 个未使用颜色资源、4 个未使用依赖
- 所有现有 21 个单元测试 + 1 个 instrumented 测试不受影响